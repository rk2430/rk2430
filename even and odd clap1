#include <stdio.h>
int
main ()
{
  int n1;
  printf ("Give a number: ");
  scanf ("%d", &n1);
  if (n1 % 2 == 0)
    printf ("%d is even.", n1);
  else
    printf ("%d is odd.", n1);

  return 0;
}

/*
output:
Give a number: 6
6 is even.
*/
